# Discord Bot Configuration
# Get your bot token from https://discord.com/developers/applications
DISCORD_BOT_TOKEN=

# Discord Bot Owner ID (your Discord user ID)
# Right-click your username in Discord (with Developer Mode enabled) and select "Copy ID"
# This is optional - if not set, owner-only commands will be disabled
DISCORD_BOT_OWNER_ID=

# Discord OAuth2 Invite URL
# Generate this from https://discord.com/developers/applications
# Select your application -> OAuth2 -> URL Generator
# Required scopes: bot, applications.commands
# Required bot permissions: Send Messages, Embed Links, Attach Files, Use Slash Commands
# This URL is used on the landing page invite button
DISCORD_INVITE_URL=

# Auto-sync slash commands on startup (default: true)
# Set to false in production to avoid rate limits
# AUTO_SYNC_COMMANDS=true

# Web API Configuration
# Host to bind the API server to (default: 0.0.0.0)
# WEB_API_HOST=0.0.0.0
#
# Port to bind the API server to (default: 8080)
# WEB_API_PORT=8080

# Storage Configuration (optional)
# Path where contact advertisements will be stored
# Can be absolute (/var/lib/coretact/storage) or relative (./data/storage)
# Relative paths are resolved from the project root
# Defaults to <project_root>/storage if not set
# STORAGE_PATH=./storage

# Logging Configuration (optional)
# Path to log file (if not set, logs only to console)
# LOG_PATH=./logs/coretact.log

# Sentry Configuration (optional)
# Sentry DSN for error tracking
# Get this from your Sentry project settings
# SENTRY_DSN=
#
# Sentry traces sample rate (default: 0.1 = 10%)
# Controls what percentage of transactions are sent to Sentry
# SENTRY_TRACES_SAMPLE_RATE=0.1
#
# Sentry profiles sample rate (default: 0.1 = 10%)
# Controls what percentage of transactions are profiled
# SENTRY_PROFILES_SAMPLE_RATE=0.1
#
# Sentry Auth Token (used by CI/CD for release tracking)
# Generate from: Sentry → Settings → Auth Tokens
# This should be kept secret and only used in GitHub Actions
# SENTRY_AUTH_TOKEN=
